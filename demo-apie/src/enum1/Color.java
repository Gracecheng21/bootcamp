package enum1;

// Monday -> String? int? char?
// Direction -> String? int? char?
// Color -> RED, BLUE, YELLOW -> String?? but how about yellow? Yellow? yello? means YELLOW?
public enum Color {
  RED, BLUE, YELLOW; // java code, if change after, you need to re-assign the class
}
